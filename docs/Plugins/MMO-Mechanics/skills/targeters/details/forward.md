# Forward Targeter

Targets a location directly in front of where the caster is facing.

## Shorthands & Aliases

- `forward`

## Options

- `d / distance`: Distance in blocks directly forward (default: 5.0).

## Example Configuration

```json5
targeter: "forward{distance=6}"
```