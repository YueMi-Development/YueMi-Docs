# Ignite Mechanic

Sets targeted entities on fire for a specified duration, or extinguishes active fire.

## Options

- `t`: Configuration parameter.
- `ticks`: Configuration parameter.
- `duration`: Configuration parameter.

## Example Configuration

```json5
mechanics: [
  "ignite{t=value,ticks=value,duration=value}"
]
```
